kind: Service
apiVersion: v1
metadata:
  name: traefik-ingress-dashboard
spec:
  selector:
    k8s-app: traefik-ingress-lb
  ports:
    - name: admin
      protocol: TCP
      port: 8080
  type: ClusterIP
