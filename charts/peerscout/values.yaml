image:
  repository: scottaubrey/data-science-dags_peerscout-api
  tag: latest

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""
  create: true

service:
  type: ClusterIP
  port: 80

ingress:
  hosts:
    - host: peerscout-api.local
      paths: []
  tls: []

resources: {}

env: {}